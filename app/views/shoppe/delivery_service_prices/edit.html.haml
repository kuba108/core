- @page_title = t('.delivery_services')
= content_for :header do
  %p.buttons= link_to t('.back_to_prices'), [@delivery_service, :delivery_service_prices], :class => 'button'
  %h2.delivery_services t('.delivery_services')
= render 'form'