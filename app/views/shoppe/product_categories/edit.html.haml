- @page_title = t('.product_categories')
= content_for :header do
  %p.buttons= link_to t('.back_to_categories_list'), :product_categories, :class => 'button'
  %h2.products= t('.product_categories')
= render 'form'